const fsubService = require('./fsub.service');
const fileService = require('./file.service');
const userService = require('./user.service');

module.exports = { fsubService, fileService, userService };
